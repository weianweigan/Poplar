<Window x:Class="BusbarReader.RvtAddin.BusbarListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:local="clr-namespace:BusbarReader.RvtAddin" d:DataContext="{d:DesignInstance Type=local:BusbarListWindowViewModel}"
        Title="BusbarListWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <ListBox ItemsSource="{Binding Busbars}"
                 SelectedItem="{Binding SelectedBusbar}"/>
            <ListBox Grid.Row="1" ItemsSource="{Binding Document.ErrorElememts}"
                     SelectedItem="{Binding SelectErrorElement}"/>
        </Grid>
            
        <ListBox Grid.Column="1"
            ItemsSource="{Binding SelectedBusbar.SortedSegments}"
                 SelectedItem="{Binding SelectedSegment}">
            
        </ListBox>
    </Grid>
</Window>
